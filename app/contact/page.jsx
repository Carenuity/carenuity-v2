import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact | Carenuity",
  description: "Get in touch with the Carenuity team.",
};

export default function ContactPage() {
  return <ContactClient />;
}
