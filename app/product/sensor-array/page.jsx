import SensorArrayClient from "./SensorArrayClient";

export const metadata = {
  title: "Sensor Array | Carenuity Products",
  description:
    "The Sensor Array Kit is a comprehensive bundle of 10 complete units based on the Shield2Go Single Adapter and DPS310 pressure sensor — perfect for IoT and sensing projects.",
};

export default function SensorArrayPage() {
  return <SensorArrayClient />;
}
